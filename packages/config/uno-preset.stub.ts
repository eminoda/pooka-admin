/**
 * UnoCSS preset stub — 安装 `unocss` 后可将此文件重命名为 `uno-preset.ts` 并实现自定义 preset。
 * 建议在根或 apps 的 vite.config 中通过 `unocss/vite` 插件加载；本包仅集中存放共享规则。
 *
 * 示例（安装依赖后）：
 * import type { Preset } from 'unocss'
 * export const pookaUnoPreset = (): Preset => ({ name: 'pooka', rules: [] })
 */
export const pookaUnoPresetPlaceholder = 'install-unocss-then-implement';
