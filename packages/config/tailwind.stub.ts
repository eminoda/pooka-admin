/**
 * Tailwind / shadcn-vue 设计令牌占位。
 * 若采用 Tailwind CSS v4 或 PostCSS 管线，可在此导出 theme 扩展并与 `packages/ui` 联动。
 * shadcn-vue 本地化后，通常会在 `packages/ui` 维护 `components.json` 与组件源码路径。
 */
export const pookaTailwindStub = true;
