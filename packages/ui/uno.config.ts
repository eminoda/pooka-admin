/**
 * UnoCSS 入口（占位）。
 * 安装 `unocss` 后改为：
 * import { defineConfig, presetUno } from 'unocss'
 * import { pookaUnoPreset } from '@pooka/config/...' // 未来从 config 包引入 preset
 *
 * export default defineConfig({ presets: [presetUno(), pookaUnoPreset()] })
 */
const _placeholder = true;
export default _placeholder;
